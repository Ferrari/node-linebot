{
  "source": "./src",
  "destination": "./doc",
  "title": "MessengerBot | ESDoc",
  "plugins": [{
    "name": "esdoc-importpath-plugin",
    "option": {
      "replaces": [{
        "from": "^src/",
        "to": "dist/"
      }]
    }
  }]
}
